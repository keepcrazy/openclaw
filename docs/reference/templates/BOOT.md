---
title: "BOOT.md 模板"
summary: "BOOT.md 的工作区模板"
read_when:
  - 添加 BOOT.md 检查清单时
---

# BOOT.md

添加简短、明确的指令，说明 OpenClaw 启动时应该执行什么操作（启用 `hooks.internal.enabled`）。
如果任务需要发送消息，请使用消息工具，然后以准确的静默 Token
`NO_REPLY` / `no_reply` 回复。
